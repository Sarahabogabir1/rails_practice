source 'https://rubygems.org'

source 'https://rubygems.org'

gem 'rails',        '4.2.2'
gem 'sass-rails',   '5.0.2' # Use SCSS for stylesheets
gem 'uglifier',     '2.5.3' # Use Uglifier as compressor for JavaScript assets
gem 'coffee-rails', '4.1.0' # Use CoffeeScript for .coffee assets and views
gem 'jquery-rails', '4.0.3' # Use jquery as the JavaScript library
gem 'turbolinks',   '2.3.0' # Turbolinks makes following links in your web application faster
gem 'jbuilder',     '2.2.3' # Build JSON APIs with ease.
gem 'sdoc',         '0.4.0', group: :doc  # bundle exec rake doc:rails generates the API under doc/api.

group :development, :test do
  gem 'sqlite3',     '1.3.9' #Use sqlite3 as the database for Active Record
  gem 'byebug',      '3.4.0' ## Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'web-console', '2.0.0.beta3'   # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'spring',      '1.1.3' # Spring speeds up development by keeping your application running in the background.
end

group :production do
  gem 'pg',             '0.17.1'
  gem 'rails_12factor', '0.0.2'
end


# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development



